# CTS on Web

Browser edition of **Comparison Timeline Studio**, prepared for deployment with GitHub Pages.

The GitHub Pages build runs as a static app and stores projects locally in the browser. Server-rendered MP4 export requires the optional CTS backend and is not available in the static-only deployment.
